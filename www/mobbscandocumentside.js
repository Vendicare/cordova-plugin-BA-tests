/*global cordova, module*/
/**
 * Enum for document's side.
 * @readonly
 * @enum {string}
 * @memberof MobbScanAPI
 */
var MobbScanDocumentSide = {
    /** Front side */
    FRONT: { value: "FRONT" },
    /** Back side */
    BACK: { value: "BACK" }
};

module.exports = MobbScanDocumentSide;
